import { createStore } from 'vuex'

export default createStore({
  state: {
  }, // 状态数据
  getters: {
  }, // 操作state状态数据的同步方法
  mutations: {
  }, // 发送请求的异步方法
  actions: {
  }, // 计算属性方法
  modules: {
  }// 字模块
})
